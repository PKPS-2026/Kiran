# Credentials — NEVER commit
config/config.py
config.py

# Uploaded data files — contain personal Aadhaar data
uploads/
*.xlsx
*.xls

# Cloudflare binary — auto-downloaded at runtime
cloudflared.exe

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd

# OS
.DS_Store
Thumbs.db
